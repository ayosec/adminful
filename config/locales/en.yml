en:
  adminful:
    home:
      title: "Admin interface"

    client:
      resource_index:
        loading: "Loading instances. Please wait..."

        batch_operations:
          delete:
            button: "Delete selected"
          none_selected: "Please select at least one instance"

        actions:
          new: "New"
          refresh: "Refresh"
          edit: "Edit"
          remove: "Remove"
          confirm_remove: "This will remove the instance. Are you sure?"

      resource_form:
        actions:
          save: "Save"
          cancel: "Cancel"
        required:
          abbr: "*"
          full: "required"
      create:
        success: "The instance has been created."
        error: "The instance could not be created."
      update:
        success: "The instance has been updated."
        error: "The instance could not be updated."

