<%=
  Rails.application.assets.index.paths.grep(/\/stylesheets$/).map do |base|
    Dir["#{base}/adminful/*.css*"].map do |filename|
       depend_on_asset filename
       Rails.application.assets.find_asset(filename).body
    end.join
  end.join
%>
